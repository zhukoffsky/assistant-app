# Что делаем дальше

План составлен 12 августа 2026, когда все десять этапов основного плана были
пройдены и проверены на устройстве. Это не продолжение плана из `CLAUDE.md`,
а отдельный список улучшений: они не были задуманы изначально и появились из
опыта живого использования.

---

## Как продолжить в новой сессии

**Сначала прочитать `CLAUDE.md` целиком.** Там контекст, архитектура,
обоснования решений и — главное — раздел 9 с перечнем ловушек, каждая из
которых уже стоила отдельного прогона на телефоне. Ниже они не повторяются,
только упоминаются.

**Состояние на момент составления плана:**

- ветка `main`, всё влито, открытых пул-реквестов нет;
- приложение собрано и стоит на Pixel 8 владельца;
- синхронизация с Google Tasks работает, консоль настроена;
- у приложения своя иконка;
- проверено на устройстве 55 пунктов раздела 8 из 77.

**Сборка и установка** (см. также раздел 7 `CLAUDE.md`):

```bash
JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home" \
ANDROID_HOME="$HOME/Library/Android/sdk" \
./gradlew --offline test lintDebug assembleDebug
```

Флаг `--offline` не обязателен — сеть с машины владельца есть. Он экономит
поход в сеть, но новую зависимость с ним не поставить.

**Устройство.** adb лежит в `~/Library/Android/sdk/platform-tools/adb`, в
PATH его нет. Кириллицу через `adb shell input text` не ввести, буфера обмена
в `cmd` нет — русские фразы набирает только владелец. Лог приложения:

```bash
~/Library/Android/sdk/platform-tools/adb logcat -d -s Magpie
```

**Порядок работы.** Каждый пункт плана — своя ветка от `main` и свой
пул-реквест, как принято в разделе 5 `CLAUDE.md`. Ветку и воркtree спрашивать
у владельца перед началом.

---

## Порядок пунктов

Владелец назвал их в порядке 2-3-4-5-6 из обсуждения; здесь они отсортированы
по нарастанию сложности.

**Сложность здесь — не объём правки, а риск сломать работающее.** Пункт с
кучей механической работы по готовому образцу дешевле короткого, который
трогает семантику будильников или схему БД: первый в худшем случае выглядит
неаккуратно и правится на глаз, второй тихо ломает то, ради чего приложение
написано, и обнаруживается через неделю.

| # | Пункт | Что делает сложным |
|---|---|---|
| ~~A~~ | ~~Уведомления на языке приложения~~ — **сделано** | Три файла, локализованный контекст, ничего сквозного. Промах — косметика: уведомление не на том языке. |
| ~~B~~ | ~~Виджет напоминаний показывает ближайшее~~ — **сделано** | Писать больше, чем в A, но всё по образцу виджета покупок: оба урока Glance уже оплачены. Ошибка видна глазом сразу. |
| ~~C~~ | ~~Отсрочка в уведомлении напоминания~~ — **сделано** | Расширяет `ReminderScheduler` и меняет семантику срабатывания. Ошибка сдвигает серию повторов — заметно не в тот же день. |
| ~~D~~ | ~~Самозакрывающаяся карточка после диктовки~~ — **сделано** | Развилка, которую решает владелец. Откат обязан быть настоящим, а гонка при закрытии экрана уже стоила потерянных позиций. |
| ~~E~~ | ~~Покупки по отделам~~ — **сделано** | Миграция Room без права на ошибку, изменение разбора фраз, обязательные тесты, настройка и группировка в UI. |

**Отсрочка и виджет поменялись местами** относительно порядка обсуждения:
виджет длиннее в написании, но безопаснее — он ничего не планирует и никого
не будит. Буквы переставлены вместе с пунктами, чтобы читались по алфавиту.

**A и B стоит делать подряд.** A даёт способ собрать строку на выбранном
языке вне Compose; виджету он нужен ровно за тем же — Glance не знает
`stringResource`, а дату показывать надо.

**D требует решения владельца, E — миграции Room**, обоим лучше достаться
свежей голове.

---

## A. Уведомления на языке приложения — **сделано 12 августа 2026**

Самое дешёвое, и заодно исправляет неточность в документации.

**Как сделано.** `core/settings/AppLocale.kt`: `forSelectedLanguage()` читает
язык из `AppearancePreferences` и отдаёт контекст с нужной локалью. Язык
читается в `MagpieNotifications`, а не в получателях, — их три, и четвёртый
забыть легко; отсюда `showDose` и `showReminder` стали `suspend`. Имена
каналов тоже следуют настройке: `MagpieApp` создаёт каналы сразу и синхронно
(канал обязан существовать до первого уведомления), а затем переименовывает
их подпиской на язык.

**Проверено на устройстве** — раздел 8 `CLAUDE.md`. Там же записан
найденный попутно дефект: диалоги приложения языку приложения не следуют.

---

**Что было.** Язык интерфейса меняется подменой конфигурации композиции,
поэтому переводится только то, что рисует Compose. Уведомления приходят на
языке системы: при английском интерфейсе — по-русски.

**Что сделать.** В `MagpieNotifications` и обоих получателях читать выбранный
язык из `AppearancePreferences` и собирать строки из контекста с нужной
локалью.

**Важно.** Здесь `createConfigurationContext` **уместен**, хотя раздел 9
`CLAUDE.md` предостерегает против него. Тот запрет касался Compose: по
цепочке `baseContext` там ищут активность, и самостоятельный контекст ронял
`rememberLauncherForActivityResult`. Строителю уведомления активность не
нужна вовсе.

**Готово, когда.** Интерфейс на English, система на русском → уведомление
приходит на английском. Плюс правка в `CLAUDE.md`: сейчас там написано, что
перевести нельзя; нельзя только подпись под иконкой.

---

## B. Виджет напоминаний показывает ближайшее — **сделано 12 августа 2026**

**Как сделано.** `ReminderDao.observeNext()` отдаёт первое по времени
невыполненное (просроченные не отсеиваются — это ровно то, что нужно
увидеть). У `ReminderRepository` появился хук `onChanged`, как у покупок,
пять мест записи. Раскладок две: `SizeMode.Responsive`, узкий размер
остаётся кнопкой — виджет мог быть положен на экран ещё ею, и в ячейку 1×1
текст не влезает никак.

Заодно оба виджета переведены на язык приложения, а смена языка их толкает:
сами они не перерисовываются и остались бы на прежнем языке до ближайшей
правки данных.

**Проверено на устройстве** — раздел 8 `CLAUDE.md`.

---

**Что было.** В `ReminderVoiceWidget` ноль текстовых элементов — это кнопка
микрофона размером с виджет. Ту же работу делают шорткат и плитка, а место на
экране он занимает как полноценный виджет.

**Что сделать.** Показывать заголовок и время ближайшего будущего
напоминания. Микрофон остаётся углом.

**Два урока Glance, уже оплаченных на виджете покупок:**

1. Данные читать **до** `provideContent` через `.first()`. Glance публикует
   первый же скомпонованный кадр, и пустое `initial` у `collectAsState`
   уезжает в лаунчер раньше, чем Room успевает ответить.
2. Толкать виджет из `ReminderRepository` при каждой записи. Подписка на Room
   обновляет виджет только пока жива сессия Glance, а она умирает вместе с
   процессом — после чего лаунчер держит последний кадр сколь угодно долго.

**Толкать есть чем.** У `ShoppingRepository` для этого заведён хук
`onChanged`: зовётся после каждой записи, а чем именно толкать — решает
`AppContainer`, так что Android в репозиторий не протекает. Тот же хук
заводится у `ReminderRepository`, мест записи пять: `add`, `update`,
`setDone`, `delete`, `onFired`.

**Дату придётся форматировать заново.** `dueLabel` — Compose-функция со
`stringResource`, в Glance она не годится. Локаль при этом нужна выбранная в
приложении, а не системная, — то есть помощник из пункта A.

**Готово, когда.** Создал напоминание → появилось в виджете; отметил
выполненным → показалось следующее; пережило перезагрузку.

---

## C. Отсрочка в уведомлении напоминания — **сделано 12 августа 2026**

**Как сделано.** Кнопки «+10 мин» и «+1 час»; `ReminderScheduler` получил
`scheduleSnooze`, а `cancel` снимает теперь оба будильника. Отсрочка
отличается от основного будильника **действием намерения**, а не кодом
запроса: extras в сравнение `PendingIntent` не входят, действие входит. По
этому же действию получатель понимает, что повтор двигать не надо.

Кнопки уведомления различаются **данными намерения** по той же причине: у доз
хватило кода запроса, потому что уведомление там одно на всё приложение, а
здесь кнопок столько же, сколько напоминаний.

Отсрочка нигде не записана — это только будильник. Перезагрузку она поэтому
не переживает, ровно как у доз.

**Проверено на устройстве** — раздел 8 `CLAUDE.md`.

---

**Что было.** У таблеток три кнопки: «Принял», «+15 мин», «+60 мин». У
напоминания одна — «Готово». Отложить нельзя, хотя это самое частое желание.

**Что сделать.** Добавить «+10 мин» и «+1 час». Механизм отсрочки полностью
написан для таблеток (`AlarmManagerMedScheduler` держит два раздельных
будильника) и переиспользуется почти целиком. Android показывает максимум три
кнопки — это и есть предел.

**Ловушка.** У таблеток отсрочка не имеет права затирать ежедневный
будильник, и это уже учтено. У напоминаний хуже: есть повторы, и отложенное
«каждый вторник» не должно сдвинуть всю серию. Нужен отдельный будильник
отсрочки, как у доз.

**Ловушка вторая, оттуда же.** `ReminderAlarmReceiver` после показа
уведомления зовёт `onFired`, а тот переводит повтор на следующее
срабатывание. Будильник отсрочки пойдёт через тот же получатель — значит, ему
нужен признак «это отсрочка», иначе одно «+10 мин» сдвинет серию на неделю
вперёд второй раз.

**Мелочь, которая всё равно всплывёт.** Код запроса у напоминания — это
`id.toInt()`; будильнику отсрочки нужен свой диапазон, иначе он затрёт
основной. И `ReminderScheduler` при этом расширяется, а его подменяют два
теста (`ReminderRepositoryTest`, `VoiceCaptureViewModelTest`) — фейки
придётся дописать.

**Готово, когда.** Отложил → пришло снова через указанное время;
повторяющееся расписание не поехало (проверяется по `dumpsys alarm`: должны
жить оба будильника сразу).

---

## D. Самозакрывающаяся карточка после диктовки — **сделано 12 августа 2026**

**Развилку владелец решил так:** автозакрытие для обоих, но у напоминания
разобранное время показано крупно и акцентом, день — словом («Завтра,
15:00»). Три секунды, полоса показывает остаток, «Отменить» откатывает.

**Как сделано.** Форма подтверждения убрана целиком. Разбор → запись сразу →
карточка → закрылась сама. Запись **до** показа карточки, а не после: так
диктовка переживает любое закрытие экрана. Откат удаляет записи, у
напоминания снимает ещё и будильник, и заканчивается до `Done` — на нём
активность закрывается и отменяет `viewModelScope`.

**Проверено на устройстве голосом** — раздел 8 `CLAUDE.md`. Там же записана
ловушка сборки из воркtree без `local.properties`.

---

**Что было.**

**Зачем.** Смысл приложения — «тап по виджету → сразу микрофон». Сейчас после
диктовки человек упирается в форму подтверждения, которую надо прочитать и
нажать «Сохранить». Разбор фраз работает достаточно хорошо, чтобы этот шаг
стал лишним. Возможность такого упрощения записана в разделе 2 `CLAUDE.md`
с самого начала.

**Развилка.** Для покупок автосохранение безопасно: не то записалось —
поправил в списке. Для напоминаний нет: неверно понятое время означает
будильник не в тот час, и обнаружится это, когда он не сработает.

Варианты: автозакрытие только для покупок · автозакрытие для обоих, но с
крупно показанным разобранным временем · автозакрытие везде.

**Что бы ни выбрали.** Отмена обязана откатывать по-настоящему: для покупок
удалить записи, для напоминаний ещё и снять поставленный будильник.

**Ловушка, уже оплаченная.** Запись должна заканчиваться **до** закрытия
экрана. `Done` до записи означает отменённую корутину и потерянные позиции —
ровно это и случилось 11 августа, из пяти покупок сохранялись три. Защита от
повторного тапа — отдельный флаг, а не смена состояния.

**Готово, когда.** Диктую → позиции в списке без единого касания; «Отменить»
в течение трёх секунд убирает их и снимает будильник.

---

## E. Покупки по отделам, за настройкой — **сделано 12 августа 2026**

**Как сделано.** Категории — закрытое перечисление `ShoppingCategory`, промпт
получает ровно его; неизвестное значение становится «Прочим». Миграция 1→2
добавляет колонку пустой и проверена на живых данных с историей приёма.
Задним числом ничего не размечается — владелец выбрал «Прочее» вместо разового
прогона старых записей через модель.

Обе находки диктовки починены здесь же: неразрывный пробел после «напомни»
(`core/util/Text.kt`) и недорезанное перечисление (пример в промпте плюс
переспрос при склейке). Подробности — раздел 9 `CLAUDE.md`.

**Проверено на устройстве** — раздел 8 `CLAUDE.md`. Там же краш, найденный
прогоном, и то, что он показал про запись на главном потоке.

---

**Что было.** Самое дорогое и единственное, что трогает базу. Владелец просил
именно переключатель в настройках, а не безусловное поведение.

**Что сделать.** LLM уже в цепочке разбора — пусть возвращает категорию
вместе с позицией. В `shopping_items` добавляется колонка, `ShoppingScreen`
группирует список по отделам, в настройках появляется галка.

Ценность в магазине выше, чем у любой скорости ввода: не мечешься по залу.

**Три подводных камня:**

1. **Миграция Room обязательна.** Схема экспортируется (`exportSchema =
   true`), а `fallbackToDestructiveMigration` теперь недопустим: 12 августа
   отдельно обсуждали, что историю приёма лекарств терять нельзя.
2. **Категорию знает только LLM.** Позиции, разобранные правилами или
   введённые с клавиатуры, останутся без неё. Нужен разряд «Прочее», и он не
   должен выглядеть ошибкой.
3. **Переключатель не действует задним числом.** Уже лежащие позиции
   категории не получат. Решить заранее: оставить их в «Прочем» или разово
   прогнать через модель.

**Сюда же — два дефекта разбора, найденных 12 августа на живой диктовке.**
Владелец отнёс их к этому пункту: всё равно трогать промпт и правила.

1. **Модель режет длинную фразу плохо.** «хлеб макароны мясо фарш куриный
   индюшиные ноги ватрушку с изюмом средство для мытья посуды» дала
   `llm, 2 item(s)`: отделилось только средство для мытья посуды, остальные
   семь товаров остались одной позицией. На коротких фразах разрез верный,
   ломается именно длинная. Это та же слабость GLM в следовании инструкциям,
   из-за которой промпту уже понадобились примеры (раздел 9 `CLAUDE.md`).
   Категории добавят модели работы на той же фразе — значит, сначала надо
   починить разрез, иначе к одной ошибке добавится вторая.
2. **В заголовке напоминания остаётся повелительный глагол.** «напомни
   съездить на рынок» так и записалось: правила вычёркивают слова о времени,
   но не «напомни» в начале. Лечится тем же чисткой-регуляркой, что и
   «купить» у покупок, — с оглядкой на ловушку `\b` и кириллицы.

**Тесты обязательны.** Пункт затрагивает разбор фраз, а по соглашениям из
раздела 5 `CLAUDE.md` парсер покрывается юнит-тестами. Обе находки выше
проверяются юнит-тестами без устройства.

**Готово, когда.** Галка включена → список сгруппирован по отделам;
выключена → плоский список как сейчас; существующие данные целы после
обновления.

---

## Что осталось непроверенным на устройстве

Не входит в план, но всплывёт при работе. Полный список — в разделе 8
`CLAUDE.md`, здесь только существенное.

- **Синхронизация проверена наполовину.** Выгрузка работает; отметка
  «выполнено», удаление задачи и поведение в самолётном режиме — нет.
- **Виджет покупок не проверялся после перезагрузки.** Остальные четыре
  пункта этапа 3 закрыты 12 августа.
- **Голосовые сценарии на русском** проверяет только владелец: через adb
  кириллицу не набрать, а говорить в микрофон некому.

---

## Чего решено не делать

Помимо списка «вне области» из раздела 1 `CLAUDE.md` — синхронизация между
устройствами, Wear OS, офлайн-распознавание, учёт остатка таблеток — отдельно
зафиксировано:

**Не публиковать в Google Play.** На этом держатся два разрешения:
`USE_EXACT_ALARM` и `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`. Play не даёт их
почти никому, и публикация заставила бы гонять пользователя по системным
настройкам после каждой установки.

**Не возвращать облачный бэкап.** Локальная БД остаётся единственным
источником правды. Но отсюда следует нерешённая проблема: копия выключена, а
экспорт истории убран, поэтому восстановить данные сейчас неоткуда.
Обсуждалось 12 августа как кандидат в план; владелец выбрал другие пять
пунктов, вопрос остаётся открытым.

---

---

## Где остановились 13 августа 2026, вечер

Всё, что описано ниже под заголовком «Где остановились 13 августа 2026»,
влито в `main` пул-реквестом #11. Дальше работа идёт в ветке
**`claude/tech-debt`** (12 коммитов от `main`, не влита, PR не открыт).
Рабочее дерево чистое, сборка и тесты зелёные, lint — `No issues found`.

### Что в этой ветке

1. **Отсрочка переживает перезагрузку.** Колонка `snoozedUntil` у
   напоминаний и приёмов, миграция Room 2→3. Просроченная стирается, а не
   показывается. Всё, что отсрочку заканчивает, стирает и её срок.
2. **Версии подняты до конца:** AGP 9.3.1, Gradle 9.7, Kotlin 2.4.10,
   `compileSdk` и `targetSdk` 37, Retrofit 3, OkHttp 5. Предупреждений lint
   не осталось (было 57). Цена — два флага отказа в `gradle.properties`,
   причина в разделе 3 `CLAUDE.md`.
3. **Иконки Material — свои файлы** в `res/drawable`, зависимость убрана.
4. **Превью виджетов** и **карточка «Быстрый доступ»** (поставить виджет или
   плитку одним тапом).
5. **«Проверка доставки» убрана** из настроек вместе с `TestAlarmScheduler`.

### Что проверить первым делом

- **Превью виджетов на самом деле не показываются.** Код верен, но система
  отвечает `accepted=false`: лимит около двух вызовов в час был выжжен
  переустановками. Открыть список виджетов и посмотреть; в логе строка
  `preview: … accepted=`. Если и через час `false` — копать дальше, а не
  верить логу без картинки.
- Диктовка длиннее полутора минут и поведение без сети, когда запись
  сделана, а расшифровать нечем.
- Напоминание голосом: оно вернулось на системный диалог и после этого не
  переспрашивалось.

### Что решено, но не сделано

- **App Functions** (вызов действий приложения ассистентом) — для этого
  приложения это была бы киллер-фича, но API экспериментальный, а
  интеграция с Gemini в закрытом превью. Заявку в Early Access подаёт
  владелец, у ассистента нет доступа к его аккаунту.
- **Резервной копии по-прежнему нет**, и восстановить историю приёма
  лекарств неоткуда. Самый крупный незакрытый пробел.

### Ловушки этого дня

1. **Ответ системы надо читать.** `setWidgetPreview` возвращает `boolean`;
   выброшенный, он превратил «не приняла, лимит» в уверенный отчёт «готово».
2. **Рабочий каталог соскакивает из воркtree в основной репозиторий.**
   Трижды за день: сборка «успешно» собирала не тот каталог, а `sed` правил
   не те файлы. Каждую команду — с явным `cd` или `git -C`.
3. **Ключи в `local.properties` нельзя дописывать через `>>`** — без
   перевода строки в конце склеиваются две строки и ломаются два ключа.
4. **Проверять сборкой, а не рассуждением о номерах версий.** Вывод «под
   Kotlin 2.4 нет KSP» был неверным и стоил трёх лишних предупреждений.

---

## Где остановились 13 августа 2026

**Пять пунктов A–E сделаны и влиты в ветку `improvements`** (семь коммитов
от `main`), там же — всё, что выросло из живого использования 12 и 13
августа. В `main` ветка ещё не влита, пул-реквеста нет.

### Что сделано сверх плана

1. **Виджет таблеток** — третий виджет: название, доза, время. Плитка дозы
   горит акцентом, **пока приём не отмечен**, и гаснет после. Направление
   успели перепутать в обе стороны, поэтому оно записано и в коде, и в
   `CLAUDE.md`.
2. **Отделы в виджете покупок** — когда галка включена.
3. **Отмена удаления убрана целиком** по просьбе владельца.
4. **Клиент LLM повторяет запрос** при обрыве и при `429`.
5. **Диктовка покупок переписана на свою запись.** Приложение пишет звук
   само (`VoiceRecorder`, AAC в MP4), расшифровывает Whisper на Cloudflare
   Workers AI одним запросом, заканчивает запись только «Готово».
   Напоминание осталось на системном диалоге.

### Проверено на устройстве 13 августа

Диктовка работает целиком: 16 секунд с паузами → расшифровка за 2,7 с →
`shopping parse: llm, 7 item(s)` → позиции разложены по отделам. Ни одного
обрыва микрофона.

### Что осталось

- **Влить `improvements` в `main` пул-реквестом.** Ветка большая, и это
  единственное, что отделяет сделанное от главной ветки.
- **Не проверено:** диктовка длиннее полутора минут (упор в предел
  записи); поведение без сети, когда запись сделана, а расшифровать нечем;
  напоминание голосом после возврата на системный диалог.
- **Открытый вопрос про резервную копию** — см. «Чего решено не делать»:
  локальная БД остаётся единственным источником правды, а экспорта больше
  нет, поэтому восстановить данные сейчас неоткуда.

### Три ловушки этого дня, которые дороже всего повторить

1. **`SpeechRecognizer` не умеет длинную диктовку, и обойти это нельзя.**
   Пять попыток, каждая закрыла свою гипотезу. Полный разбор — в разделе 3
   `CLAUDE.md`, «Диктовка»; там же выключатель непрерывной сессии с
   описанием, почему он выключен.
2. **Ключи в `local.properties` нельзя дописывать через `>>`.** Файл без
   перевода строки в конце склеивает строки, и ломаются сразу два ключа, а
   выглядит это как «провайдер не принимает верный ключ».
3. **Рабочий каталог соскакивает из воркtree в основной репозиторий.**
   Дважды за день сборка «успешно» собирала не тот каталог, показывая
   `UP-TO-DATE` на только что изменённый код. Каждую команду — с явным
   `cd` или `git -C`, и сверять `pwd`, когда результат странный.

**Сборка из воркtree требует своей копии `local.properties`** — он в
`.gitignore` и в воркtree не приезжает. Нужны три ключа: `zaiApiKey`,
`cloudflareAccountId`, `cloudflareApiToken`.
