<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!--
      USE_EXACT_ALARM выдаётся системой без участия пользователя, но Google
      Play разрешает его только будильникам и календарям. Приложение в Play
      не публикуется, поэтому берём его: иначе пришлось бы гонять владельца
      в системные настройки за SCHEDULE_EXACT_ALARM после каждой установки.
      На Android 12 (API 31–32) USE_EXACT_ALARM ещё не существует, там
      работает SCHEDULE_EXACT_ALARM — он на этих версиях выдаётся сразу.
    -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission
        android:name="android.permission.SCHEDULE_EXACT_ALARM"
        android:maxSdkVersion="32" />

    <!--
      То же рассуждение, что и с USE_EXACT_ALARM: Play это разрешение почти
      никому не даёт, но приложение в Play не публикуется.

      Без него остаётся только ACTION_IGNORE_BATTERY_OPTIMIZATION_SETTINGS —
      общий список всех приложений, где себя надо искать по алфавиту, и где
      переключатель «Оптимизировано / Без ограничений» легко спутать с
      «Ограничить фоновую активность». Это разные вещи: в белый список Doze
      кладёт только «Без ограничений», а проверка читает именно его. С этим
      разрешением доступен адресный диалог на один тап, который ставит ровно
      тот флаг, что проверяется.
    -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!--
      Android 11+ прячет от приложения чужие пакеты. Без этого блока
      распознаватель речи не виден и запуск падает в ActivityNotFoundException.

      Разрешение RECORD_AUDIO не объявлено намеренно: записывает
      приложение-распознаватель в своём процессе, нам микрофон не нужен.
    -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <!--
      Атрибуты для новых API объявлены намеренно: на старых устройствах они
      просто игнорируются, а на новых нужны — `localeConfig` включает системный
      экран «Язык приложения», без которого уведомления не перевести вовсе.

      `fullBackupContent` в пару к `dataExtractionRules` lint просит зря:
      до Android 12 всё закрывает `allowBackup="false"`, и правила оттуда
      вообще не читаются.
    -->
    <application
        android:name=".MagpieApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:enableOnBackInvokedCallback="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Magpie"
        tools:ignore="UnusedAttribute,DataExtractionRules">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Magpie">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!--
          Отдельная задача и excludeFromRecents: быстрый ввод не должен
          оседать в списке недавних и мешать стеку главного экрана.
        -->
        <!--
          exported="true" нужен статическим шорткатам: лаунчер запускает их
          от своего имени. Цена — любое приложение может открыть экран
          диктовки. Ничего чувствительного он не показывает и ничего не
          пишет без подтверждения, так что размен допустимый.
        -->
        <activity
            android:name=".core.voice.VoiceCaptureActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@style/Theme.Magpie.Transparent">
            <intent-filter>
                <action android:name="com.zhukoffsky.magpie.action.CAPTURE_SHOPPING" />
                <action android:name="com.zhukoffsky.magpie.action.CAPTURE_REMINDER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <service
            android:name=".core.voice.tile.ReminderVoiceTileService"
            android:exported="true"
            android:icon="@drawable/ic_mic"
            android:label="@string/tile_reminder"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".core.voice.tile.ShoppingVoiceTileService"
            android:exported="true"
            android:icon="@drawable/ic_mic"
            android:label="@string/tile_shopping"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <receiver
            android:name=".feature.reminders.alarm.ReminderAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".feature.reminders.alarm.ReminderActionReceiver"
            android:exported="false" />

        <receiver
            android:name=".core.diagnostics.TestAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".feature.meds.alarm.MedAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".feature.meds.alarm.MedActionReceiver"
            android:exported="false" />

        <receiver
            android:name=".feature.reminders.alarm.BootCompletedReceiver"
            android:enabled="true"
            android:exported="true"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".feature.shopping.widget.ShoppingWidgetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/shopping_widget_info" />
        </receiver>

        <receiver
            android:name=".feature.meds.widget.MedWidgetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/med_widget_info" />
        </receiver>

        <receiver
            android:name=".feature.reminders.widget.ReminderVoiceWidgetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/reminder_widget_info" />
        </receiver>
    </application>

</manifest>
