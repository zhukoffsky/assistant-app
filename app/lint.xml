<?xml version="1.0" encoding="utf-8"?>
<!--
  Подавления с адресом до файла или папки. Всё, что можно закрыть аннотацией
  рядом с кодом, закрывается там — сюда попадает только то, к чему аннотацию
  не прикрепить.
-->
<lint>

    <!--
      Совет верный по букве и нерабочий на практике: при переименовании папки
      в `mipmap-anydpi` AAPT перестаёт находить `ic_launcher` вовсе, и сборка
      падает на `processDebugResources`. Проверено на AGP 8.7.3 с чистой
      сборкой. Квалификатор `v26` при minSdk 29 лишний, но безвредный, а
      работающая иконка дороже одного предупреждения.
    -->
    <issue id="ObsoleteSdkInt">
        <ignore path="src/main/res/mipmap-anydpi-v26" />
    </issue>

</lint>
