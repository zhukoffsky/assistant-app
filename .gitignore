*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
# Без слеша в начале — иначе правило ловит только корневой build/, а вывод
# сборки лежит в app/build/ и попадает в git status при каждой сборке.
build/
/captures
.externalNativeBuild
.cxx
local.properties
# Сессии компилятора Kotlin: заводятся при каждой сборке и живут в корне.
.kotlin/

# Ключи и подписи — никогда не попадают в репозиторий
*.jks
*.keystore
google-services.json
secrets.properties
