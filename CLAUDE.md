# Magpie / Сорока — голосовой помощник

Личное Android-приложение для быстрого голосового ввода: напоминания, приём
таблеток, список покупок. Главная ценность — **скорость вызова**: тап по
виджету/плитке → сразу микрофон, без промежуточных экранов.

Статус: **план утверждается, код не начат.**

---

## 1. Контекст проекта

- **Пользователь:** один основной, устройство Pixel 8 (свежий Android).
  Возможна раздача APK членам семьи — у каждого свои данные, обмена между
  устройствами нет.
- **Уровень владельца:** уверенное программирование, Android/Kotlin — с нуля.
  В обсуждениях подробно объяснять Android-специфику (жизненные циклы,
  разрешения, ограничения фона); базовое программирование объяснять не нужно.
- **Сборка:** Android Studio локально + GitHub Actions (debug-APK артефактом).
- **Публикации в Google Play нет**, установка APK напрямую.

### Явно вне области проекта

Свой бэкенд · синхронизация между устройствами · двусторонняя синхронизация с
Google · Wear OS и смарт-браслеты · Google Calendar · аналитика и
краш-репортинг · учёт остатка таблеток · офлайн-распознавание речи.

---

## 2. Требования

### Общее
- Локальная БД — единственный источник правды.
- Интерфейс на русском и английском (`values-ru/` + `values/`).
- Тип записи определяется **точкой входа**, а не содержанием фразы:
  виджет покупок → покупка, вход напоминаний → напоминание. Автоматической
  классификации нет.
- Распознавание речи — только онлайн, системное (`RecognizerIntent`).
  Без сети функция голоса недоступна, это ожидаемое поведение.
- **Разрешение `RECORD_AUDIO` приложению не нужно** и не объявляется: при
  запуске `RecognizerIntent` записывает приложение-распознаватель в своём
  процессе. Разрешение потребуется только при переходе на `SpeechRecognizer`
  с собственным интерфейсом. В манифесте вместо этого нужен блок `<queries>`
  с `android.speech.RecognitionService` — иначе на Android 11+ распознаватель
  не виден и запуск падает.
- После распознавания — экран подтверждения с редактируемыми полями.
  (Возможное упрощение в будущем: карточка на 3 секунды с автозакрытием.)

### Напоминания
- Одноразовые («завтра в 9 позвонить в поликлинику») и повторяющиеся
  («каждый вторник вынести мусор»).
- Событий с длительностью нет — это не календарь.
- Синхронизация: **односторонняя** запись в Google Tasks. Обратное чтение,
  разрешение конфликтов и обработка удалений на стороне Google не делаются.

### Таблетки
- Один курс, одно лекарство, **один приём в сутки в фиксированное время**.
- Дозы чередуются по циклу. Модель общая: список доз произвольной длины,
  повторяемый по кругу (`[100, 75]` — частный случай сценария владельца).
- **Индекс дозы вычисляется из даты, а не хранится счётчиком:**
  `doseIndex = daysBetween(startDate, today) mod doses.size`.
  Счётчик «следующая доза» разъезжается при пропуске, перезагрузке или ручной
  правке истории и приводит к неверной дозировке. Это архитектурное
  требование, а не деталь реализации.
- Уведомление: три кнопки — «Принял», «+15 мин», «+60 мин» (Android
  показывает максимум три). Полный набор отсрочек (5/10/15/30/60) — внутри
  приложения.
- Пропуск дозы — неприятно, но не критично. Следствие: обычные уведомления и
  `setExactAndAllowWhileIdle`; полноэкранные будильники, звук поверх
  беззвучного режима и эскалация не нужны.
- История: список «принял / пропустил» + отметка приёма задним числом.

### Список покупок
- Голосовое добавление, отметка «куплено», отдельный виджет.
- Общего доступа с другими телефонами нет.

### Разбор фраз
- Гибрид: сначала правила, при неудаче — LLM. Обе реализации за общим
  интерфейсом (`PhraseParser` у напоминаний, `ShoppingItemsParser` у
  покупок), взаимозаменяемы.
- **Покупки разбирает та же связка, и без неё они не работают.** Правила
  режут по знакам препинания, а распознавание речи их не ставит вовсе —
  «молоко хлеб яйца» приезжает одной строкой. Резать по пробелам нельзя:
  «хлеб бородинский» распался бы надвое. Границы товаров определяет тот, кто
  понимает язык, поэтому к модели уходит любая фраза без `,` `;` и перевода
  строки. Союз «и» надёжной границей не считается: он встречается и внутри
  перечисления.
- LLM: GLM-4.7-Flash от Z.ai (модель `glm-4.7-flash`) прямым HTTP-вызовом на
  OpenAI-совместимый `chat/completions`. Провайдер выбран по доступности, а не
  по качеству: владелец ходит в сеть из России без VPN, а Anthropic, Google
  AI Studio, Groq и GitHub Models на российские IP не отвечают. Модель
  бесплатна навсегда, карта при регистрации не нужна.
- Клиент OpenAI-совместимый, поэтому смена провайдера стоит двух констант в
  `OpenAiCompatApi` — так же подключаются Cloudflare Workers AI, Cerebras,
  Mistral, OpenRouter и YandexGPT. Резерв на случай слабого русского —
  GigaChat, но он требует корневой сертификат НУЦ Минцифры и OAuth вместо
  статического ключа.
- **Ключ один на всех и берётся только из сборки.** Он лежит в
  `local.properties` (файл в `.gitignore`) и попадает в
  `BuildConfig.LLM_API_KEY`. Ввести его в приложении негде: поля в настройках
  нет, хранения в DataStore нет.
- Сборка без ключа — штатный случай (CI, чужая машина). Тогда `LLM_API_KEY`
  пустой, `HybridPhraseParser` получает `llm = null`-ключ и работает на одних
  правилах: фраза не теряется, просто получает время по умолчанию.
- **В коде и в репозитории ключей нет — ни при каких обстоятельствах.**
  Захардкоженный в `.kt` ключ уехал бы в историю git, откуда его уже не
  вычистить, и в каждый розданный APK.

---

## 3. Стек и архитектура

Один Gradle-модуль, пакеты по функциям.

```
UI (Compose)  →  ViewModel  →  Repository  →  Room DAO
                                          →  GoogleTasksApi (Retrofit)
Widget / Tile / Shortcut  →  VoiceCaptureActivity  →  PhraseParser  →  Repository
AlarmManager  →  BroadcastReceiver  →  NotificationManager
```

| Область | Решение |
|---|---|
| Язык / UI | Kotlin, Jetpack Compose, Material 3 |
| Оформление | Своя палитра «тёплое стекло» поверх Material 3, см. `core/ui/theme/` |
| БД | Room |
| DI | Ручной `AppContainer` в `Application` (не Hilt) |
| Виджеты | Glance |
| Будильники | `AlarmManager.setExactAndAllowWhileIdle` + пересоздание по `BOOT_COMPLETED` |
| Фоновые задачи | WorkManager (синхронизация с ретраями) |
| Речь | `RecognizerIntent` (системный диалог распознавания) |
| Сеть / JSON | Retrofit + kotlinx.serialization |
| Google OAuth | Credential Manager + Authorization API, обращение к Tasks REST API напрямую |
| Настройки | DataStore (Preferences) |

Версии зафиксированы консервативно (AGP 8.7.3, Kotlin 2.0.21, Gradle 8.9,
compileSdk/targetSdk 35, minSdk 29). Причина: собрать проект локально в среде
ассистента невозможно (Maven-репозиторий Google недоступен из контейнера),
проверка идёт через GitHub Actions, поэтому заведомо совместимый набор версий
дороже свежести. Поднимать версии — на этапе 10 или когда Android Studio
предложит.

### Обоснование неочевидных выборов

- **Один модуль.** Многомодульность окупается скоростью сборки в команде; для
  соло-проекта это налог на конфигурацию Gradle. Разделение возможно позже.
- **Ручной DI вместо Hilt.** BroadcastReceiver'ы, Glance-виджеты и
  `TileService` получают зависимости из ручного контейнера тривиально, тогда
  как с Hilt в них регулярно возникают проблемы. Плюс отсутствие
  кодогенерации важно при первом знакомстве с Android. Переход на Hilt
  впоследствии — механическая работа.
- **Room, а не SQLDelight.** Больше документации и примеров.
- **Retrofit, а не `google-api-services-tasks`.** Официальная библиотека —
  тяжёлая Java-обёртка с блокирующими вызовами, неудобная на Android.
- **`setExactAndAllowWhileIdle`, а не `setAlarmClock`.** Последний надёжнее в
  Doze, но показывает иконку будильника в статус-баре и оправдан только при
  критичности пропуска дозы, которой здесь нет.

### Оформление: «тёплое стекло»

Файлы: `core/ui/theme/` (палитра, скругления, движение, типографика),
`core/ui/MagpieSurfaces.kt` (фон и стекло), `core/ui/WindowBlur.kt`.

- **Стекло — это заливка, а не размытие, и так будет всегда.** В Compose нет
  backdrop-blur: `Modifier.blur` размывает собственное содержимое элемента, а
  не то, что под ним. Поэтому `GlassSurface` — полупрозрачная заливка с
  рамкой в 1dp поверх заведомо мягкого градиентного фона; глаз читает это как
  матовое стекло. Это не уступка старым версиям — другого способа нет ни на
  какой.
- **Единственное исключение — прозрачное окно поверх чужого экрана.** Задник
  `VoiceCaptureActivity` размывает система (`FLAG_BLUR_BEHIND`), потому что
  окно целиком это умеет, а элемент внутри — нет.
- **Решает не `SDK_INT`, а `addCrossWindowBlurEnabledListener`.** Система
  гасит оконное размытие при энергосбережении, при девелоперском флаге и на
  устройствах без поддержки в SurfaceFlinger — причём на ходу. Проверка
  версии дала бы ложное «да», и вместо карточки осталась бы полупрозрачная
  плашка поверх нечитаемого экрана. Нет размытия — плотное притемнение и
  непрозрачная карточка.
- **Карточка диктовки непрозрачная всегда, размытие тут ни при чём.** Стекло
  на ней пробовали дважды и дважды вернули. Расчёт был на то, что размытие
  снимает контрастные края и сквозь заливку в 13% белого ничего не
  просвечивает. На Pixel 8 просвечивает: замер по скриншоту дал внутри одной
  карточки `#37302D` там, где за ней пустой фон, и `#684E43` там, где за ней
  плавающая навигация. Причина в том, что размывается **чужой** экран, а не
  мягкий фон приложения: у навигации своя заливка и своя яркость, размытие её
  приглушает, но не убирает.
- **Роли `surfaceContainer*` в схеме заполняются обязательно.** Их берут
  диалоги, пикеры и меню Material 3 — не `surface`. Оставленные пустыми, они
  падают на базовую палитру Material: на тёплом персиковом фоне всплывала
  сиреневая карточка `#ECE6F0`, в тёмной теме — холодная серо-синяя
  `#2B2930`. Диалоги при этом **непрозрачные**, они именно не того цвета;
  глаз читает холодную подложку на тёплом фоне как полупрозрачную, и на этом
  легко потерять полчаса. Проверять пипеткой, а не на глаз.
- **Виджетам стекло недоступно в принципе.** Glance рисует RemoteViews в
  процессе лаунчера: ни размытия, ни произвольных кистей, только плоская
  заливка. Виджет держит узнаваемость палитрой и типографикой.
- **Один акцент на всё приложение.** Оранжевый несёт только действие — кнопку,
  активную вкладку, принятую дозу. Остальное держится на прозрачности и
  тени, а не на цвете.
- **Движение задаётся пружинами, а не длительностями.** Три пружины на всё
  приложение (`MagpieMotion`); каждая лишняя расползается по экранам в виде
  «почти такой же».
- **Два источника цвета сосуществуют намеренно.** Текст и поверхности берутся
  из `MagpieTheme.colors` (свои роли: `ink`, `ink2`, `glass`, `warn`), а
  акцент и элементы Material — из `MaterialTheme.colorScheme`, который
  заполнен теми же цветами. Второе нужно, чтобы готовые компоненты Material 3
  не выпадали из палитры.

### Модель данных (черновик)

- `reminders` — id, title, dueAt, repeatRule, isDone, remoteTaskId, syncState, createdAt
- `shopping_items` — id, title, isChecked, position, createdAt, remoteTaskId
- `med_courses` — id, name, doses (список мг), timeOfDay, startDate, isActive
- `med_intakes` — id, courseId, scheduledAt, takenAt, status, doseMg, snoozeCount

Поля `remoteTaskId` / `syncState` закладываются с самого начала, даже до
реализации синхронизации, чтобы не мигрировать схему на этапе 8.

---

## 4. План разработки

Каждый этап даёт APK, который можно поставить на телефон и проверить руками.

| № | Этап | Критерий приёмки |
|---|---|---|
| 0 | Каркас: проект, Compose, Room, навигация, тема, строки RU/EN, CI собирает debug-APK | Приложение ставится, открывается, показывает три пустых раздела |
| 1 | Список покупок: БД, добавление, отметка «куплено», удаление | Работающий список с клавиатурным вводом |
| 2 | Голосовой конвейер: `VoiceCaptureActivity`, распознавание, разбор фразы, экран подтверждения | «Молоко, хлеб, яйца» голосом → элементы в списке |
| 3 | Виджет покупок (Glance): список, кнопка микрофона, тап = отметить | Виджет на домашнем экране, тап по микрофону → запись |
| 4 | Напоминания: модель, парсер дат на правилах, `AlarmManager`, уведомления, повторы, восстановление после перезагрузки | Уведомление приходит вовремя и переживает перезагрузку |
| 5 | Быстрые точки входа: плитка в шторке, App Shortcuts, виджет-кнопка напоминаний | Из шторки в микрофон одним тапом при заблокированном экране |
| 6 | Таблетки: курс, цикл доз, ежедневный будильник, уведомление с тремя кнопками, история, отметка задним числом | Полный цикл за пару дней: сработало → отложил → принял → в истории |
| 7 | Экран самодиагностики разрешений + тестовое уведомление через минуту | Видно, что не разрешено, и чинится на месте |
| 8 | Google Tasks: OAuth, синхронизация через WorkManager, экран настройки, обработка отзыва доступа | Напоминание видно в Google Tasks на компьютере |
| 9 | LLM-парсер: поле ключа в настройках, стратегия «правила → LLM» | Фраза, не осиленная правилами, разбирается корректно |
| 10 | Полировка: английская локализация, тёмная тема, UX | — |

Естественные точки остановки: после этапа 3 (полезное приложение уже есть) и
после этапа 7 (всё работает локально, Google опционален).

**Все десять этапов пройдены.** Дальнейшие работы — в
`docs/next-steps.md`: пять улучшений, которых не было в исходном плане, они
выросли из живого использования. Там же — как войти в проект новой сессией.

---

## 5. Соглашения

### Код
- Kotlin official code style, 4 пробела, ktlint-совместимое форматирование.
- Пакеты по функциям: `feature/<name>/{ui,data,domain}`, общее — в `core/`.
- ViewModel отдаёт единый immutable `UiState` через `StateFlow`; события UI —
  через методы ViewModel, не через колбэки в composable.
- Composable-функции не обращаются к репозиториям и `Context` напрямую.
- Строки — только в ресурсах, хардкод текста в UI запрещён (двуязычность).
- Публичные точки входа (Activity, Receiver, Worker, Tile, Widget) —
  максимально тонкие: вытащить зависимости, вызвать репозиторий, выйти.

### Тесты
- Обязательны юнит-тесты на две вещи: **парсер фраз** и **вычисление индекса
  дозы**. Это чистая логика без Android, ломается тихо и стоит дорого.
- UI-тесты не пишем.

### Git
- Главная ветка — `main`, она же дефолтная на GitHub. Работа идёт в ветках от
  неё и возвращается пул-реквестом.
- Один коммит — один законченный этап или одна осмысленная правка.
- Сообщения коммитов на английском, императив: `Add shopping list widget`.

### Безопасность
- В репозитории нет ключей, токенов и keystore-файлов.
- API-ключ LLM-провайдера — только в `local.properties` под именем
  `zaiApiKey`, откуда сборка кладёт его в `BuildConfig.LLM_API_KEY`. Файл вне
  git.
- Вшитый в сборку ключ всё равно достаётся из APK разбором — это защита
  репозитория, а не самого ключа. Для бесплатного ключа Z.ai приемлемо; для
  платного провайдера так делать не стоит.
- `local.properties`, `*.jks`, `*.keystore` — в `.gitignore`.
- Для OAuth нужен стабильный keystore: хранится локально у владельца, в git не
  попадает; SHA-1 отпечатки debug- и release-ключей регистрируются в Google
  Cloud Console.

---

## 6. Открытые вопросы

- Название и package (`com.zhukoffsky.magpie`) — подтверждается владельцем.
  Менять после этапа 0 дорого.
- `minSdk` / `targetSdk` — определяются на этапе 0 по актуальным версиям
  (ориентир: `minSdk 29`).
- ~~Настройка Google Cloud Console перед этапом 8.~~ **Выполнена
  11 августа 2026**, синхронизация работает. Доступа к консоли у ассистента
  по-прежнему нет, поэтому всё, что там меняется, — на владельце: в
  частности, каждый новый аккаунт нужно добавлять в Test users.
- Экран подтверждения после диктовки может быть упрощён до
  самозакрывающейся карточки, если полная форма окажется медленной.

---

## 7. Как собирать

```bash
./gradlew assembleDebug      # APK в app/build/outputs/apk/debug/
./gradlew test               # юнит-тесты
```

**Локально из ассистента собирать можно** (уточнение от 10 августа 2026,
Claude Code на Mac владельца). Нужны три вещи: JDK из Android Studio,
`ANDROID_HOME` и флаг `--offline`, чтобы Gradle не ходил в `dl.google.com`, а
брал всё из `~/.gradle/caches`:

```bash
JAVA_HOME="/Applications/Android Studio.app/Contents/jbr/Contents/Home" \
ANDROID_HOME="$HOME/Library/Android/sdk" \
./gradlew --offline test assembleDebug
```

`local.properties` в репозитории нет и быть не должно — отсюда `ANDROID_HOME`
переменной, а не файлом. Без `JAVA_HOME` берётся системный JDK 26,
несовместимый с Gradle 8.9.

**Про `--offline` (уточнение от 11 августа 2026).** Сеть с Mac владельца
есть: `./gradlew lintDebug` без флага спокойно скачал `lint-gradle` и
отработал. Флаг остаётся в команде выше как быстрый путь — он экономит поход
в сеть на каждой сборке, — но он не обязателен, и **новую зависимость или
задачу, которой нет в кэше, надо запускать без него**. Именно на этом
спотыкался `lint`: в offline-кэше `lint-gradle` отсутствует, отчего казалось,
что lint запустить нельзя вовсе.

Сборка в контейнерной среде ассистента по-прежнему невозможна:
Maven-репозиторий Google (`dl.google.com`)
закрыт сетевой политикой контейнера. Проверка изменений идёт через GitHub
Actions (`.github/workflows/build.yml`), APK доступен артефактом сборки.

## 8. Проверка на устройстве (накапливается)

Список пополняется каждым этапом; сверка — в конце, перед остановкой на
этапе 7.

**Прогон 10 августа 2026 на Pixel 8 (Android 17, API 37), сборка из Android
Studio.** Отмечено `[x]` — проверено на устройстве через adb. Осталось
`[ ]` — либо требует рук владельца (речь в микрофон, перетаскивание виджета,
консоль Google), либо не проверялось.

**Отметки этапов 0–10 сделаны до редизайна 11 августа.** Функциональность с
тех пор не менялась, а внешний вид изменился весь, поэтому `[x]` в них
означает «работает», но не «выглядит как сейчас». Проверка нового вида —
отдельным списком ниже.

Чего adb не умеет и что остаётся владельцу:
- **сказать фразу в микрофон** — распознавание запускается, но говорить некому;
- **прокрутить экран, не задев переключателей**: `input swipe` по экрану с
  сегментированными переключателями умудряется их переключить. 10 августа три
  свайпа по «Настройкам» сбросили тему и язык на «Как в системе». Проверять
  настройки после прокрутки, а не считать, что свайп безобиден;
- **разблокировать телефон** отпечатком после перезагрузки;
- **положить виджет на домашний экран** и подержать иконку ради шорткатов;
- **настроить Google Cloud Console** и ввести ключ Z.ai.

Две ловушки прогона, чтобы не переоткрывать их заново:
- `adb shell input text` не умеет кириллицу и режет строку по пробелам —
  русские фразы набрать нельзя, а `"a b"` доходит как `a`. Пробелы — `%s`.
  Быстрый набор вдобавок путает буквы: «reboot test» приезжает «rebooestt».
- `adb shell am force-stop` снимает все будильники приложения. Один такой
  вызов посреди проверки Doze обнулил тест, который шёл двадцать минут.

### Виджет напоминаний показывает ближайшее

**Прогон 12 августа 2026 на Pixel 8.** Виджет к тому моменту уже лежал на
домашнем экране ячейкой 1×1 — владелец растянул его руками, adb этого не
умеет.

- [x] Растянутый виджет показывает ближайшее невыполненное: заголовок и
      «12 авг., 09:00», микрофон справа.
- [x] Отметил ближайшее выполненным → на виджете появилось следующее.
      Снял отметку → вернулось прежнее. То есть хук записи работает.
- [x] Микрофон открывает распознавание с вопросом «О чём напомнить?».
- [x] Тап по тексту открывает приложение.
- [x] Виджеты следуют языку приложения: при English на виджете
      «12 Aug, 09:00» и «Shopping», при «как в системе» — снова русский.
      **Обновление не мгновенное**: между нажатием и перерисовкой уходит
      несколько секунд, на трёх секундах ожидания проверка ещё врёт.
- [ ] Узкий виджет 1×1 остаётся кнопкой. Раскладка написана, но на
      устройстве не наблюдалась: виджет уже растянут, сжимать обратно —
      снова руки.
- [ ] Виджет переживает перезагрузку.

### Уведомления на языке приложения

**Прогон 12 августа 2026 на Pixel 8**, система по-русски, интерфейс
переключался в приложении.

- [x] Интерфейс English → тестовое уведомление пришло английским
      («Test notification / Delivery works…») при русской системе.
- [x] Настоящее напоминание тем же путём: кнопка в уведомлении — «Done».
- [x] Имена каналов в системных настройках переехали на English
      («Medication», «Reminders») — при том что сам экран Android русский.
- [x] Обратно на «Как в системе» → уведомление снова русское
      («Тестовое уведомление»). То есть следует настройке, а не залипает.
- [ ] Уведомление о приёме таблетки на выбранном языке. Тот же код и тот же
      вызов, что у напоминания, но глазами не смотрели.

**Найдено попутно и починено там же:** диалоги языку приложения не следовали
— при английском интерфейсе правка напоминания открывалась по-русски, вместе
с датой «12 авг. 2026». Проверено после починки:

- [x] Правка напоминания: «Edit reminder», «What to remind about»,
      «12 Aug 2026», Cancel/Done.
- [x] Пикер даты целиком: «Select date», «August 2026», буквы дней недели.
- [x] Пикер времени: подписи кнопок.
- [ ] Пикеры на вкладке таблеток. Тот же общий код, глазами не смотрели.

### Редизайн, настройки внешнего вида, размытие

**Прогон 11 августа 2026 на Pixel 8.** Проверено через adb со скриншотами;
там, где речь о цвете и прозрачности, — замером пикселей, а не на глаз.
Глаз здесь обманывает: холодная серая подложка на тёплом фоне читается как
полупрозрачная, хотя альфы в ней нет вовсе.

- [x] Все четыре вкладки в светлой и тёмной теме: градиентный фон, стекло и
      плавающая навигация читаются, текст нигде не сливается с подложкой.
- [x] Смена темы в настройках применяется сразу и переживает перезапуск.
      Активность при этом не пересоздаётся.
- [x] Смена языка в настройках **не вызывает мигания и пересоздания**.
      Проверено на Android 17 — том самом, где всплывал цикл.
- [x] «Настройки» открываются при явно выбранном языке и не падают.
      Регрессия `No ActivityResultRegistryOwner was provided` закрыта.
- [x] ~~Уведомления остаются на языке системы: при английском интерфейсе
      тестовое пришло по-русски.~~ **Изменено 12 августа:** уведомления
      следуют языку приложения, см. проверку ниже.
- [x] Карточка диктовки: задник размыт, за ней узнаётся тот самый список.
- [x] С выключённым размытием карточка непрозрачна, притемнение плотное.
      Проверялось девелоперским флагом `disable_window_blurs`, и
      переключение произошло **на ходу**, без перезапуска приложения, —
      то есть слушатель отрабатывает ровно так, как задумано.
- [x] Радиус 24dp: ближе к сильному краю, но список за карточкой узнаётся.
- [x] Виджеты в новой палитре выглядят прилично на тёмных обоях. На светлых
      не смотрели — обои на устройстве тёмные.
- [x] Каскадное появление списка играет один раз и не повторяется при
      отметке «куплено»: соседние строки остаются на месте, едет только
      сама отмеченная.
- [ ] Как всё это выглядит **человеческим глазом**, а не по скриншотам.
      Диктовка голосом с полной карточкой подтверждения тоже не проверена:
      через adb говорить некому.

### Этап 0 — каркас
- [x] APK ставится на Pixel 8 и открывается.
- [ ] Иконка на домашнем экране выглядит прилично (адаптивная, тёмная тема).
- [x] Переключение трёх вкладок работает, состояние вкладки не теряется.
- [x] Смена языка на английский переключает интерфейс: пройдены все четыре
      вкладки, непереведённого не осталось. Проверено пер-аппным локалем
      (`cmd locale set-app-locales`), а не сменой языка всей системы;
      название на лаунчере не смотрели.

### Этап 1 — список покупок
- [x] Добавление с клавиатуры, Enter добавляет и не закрывает клавиатуру.
- [x] Чекбокс зачёркивает позицию, счётчик купленного считает верно.
- [x] Свайп удаляет позицию; «Убрать купленное» удаляет только отмеченные.
- [x] Пустое состояние показывается на пустом списке.

### Этап 2 — голосовой ввод
- [x] Тап по микрофону открывает распознавание **без** запроса разрешений.
      Сверху оказывается активность Google-распознавателя, диалога разрешений
      нет вовсе — расчёт на чужой процесс с микрофоном оправдался.
- [x] Надиктованная фраза становится отдельными позициями. «купить хлеб
      масло колбасу немножко хлеба и наверное ещё фарш» → пять полей.
- [x] Правка и удаление позиции в карточке работают, «Сохранить» пишет в
      список — все позиции, а не часть.
- [x] Отмена распознавания даёт сообщение с «Повторить», а не тихое закрытие.
- [x] Экран за карточкой виден (активность прозрачная).
- [ ] Приложение не появляется дважды в списке недавних.
- [x] **Открытый вопрос про запятые закрыт, и решён (11 августа).**
      Распознаватель их действительно не ставит: «морковку картошку воды
      также» пришло сплошной строкой. Разбирает теперь LLM — подробности в
      разделе 9.

### Этап 10 — полировка
- [x] Свайп-удаление в покупках предлагает «Отменить», и отмена действительно
      возвращает запись. В напоминаниях свайп не проверяли.
- [x] Тап по напоминанию открывает правку: заголовок, дата, время.
      Исправленное время срабатывает верно: перенос с 13:08 на 13:30 дал
      уведомление в 13:30:00.038, причём на заблокированном экране.
- [x] Время выбирается пикером (циферблат Material). Дату начала курса
      пикером не открывали.
- [x] Тёмная тема выглядит прилично на всех четырёх вкладках.
- [x] Английский интерфейс: пройдены все четыре вкладки, непереведённого не
      нашлось.

### Этап 9 — LLM-парсер
**Прогон 10 августа 2026, ключ Z.ai из `local.properties`.**

- [ ] Без ключа всё работает как раньше, сетевых вызовов нет. Не проверялось:
      ключ вшит в сборку, для проверки нужен APK без `zaiApiKey`.
- [x] **`api.z.ai` отвечает с телефона без VPN** — ради этого провайдер и
      менялся. `parse: llm` в логе, напоминание с разумным временем.
- [x] Обычная фраза по-прежнему разбирается мгновенно: «in 5 minutes call the
      clinic» дала `parse: rules` и будильник ровно через пять минут, без сети.
- [x] Фраза без времени уходит в LLM и разбирается корректно: «book a dentist
      appointment soon» → заголовок «book dentist appointment», сегодня 19:00.
      Слово о времени из заголовка вычеркнуто.
- [x] При выключенном интернете кривая фраза не теряется: в авиарежиме
      «water the plants sometime» дала `llm: call failed` → `parse: fallback`
      → напоминание на завтра 09:00.
- [ ] Между распознаванием и карточкой видно «Разбираю время…». Не смотрели:
      проверялся ввод с клавиатуры, а не диктовка.
- [ ] Русская фраза целиком на устройстве. Через adb кириллицу не набрать;
      разбор русского проверен запросами к API напрямую (см. раздел 9), в
      приложении — нет.

### Этап 8 — Google Tasks
**Проверяется только на локальной сборке из Android Studio.** APK из CI
подписан ключом, который раннер генерирует заново каждый запуск, поэтому его
SHA-1 не совпадает с зарегистрированным и авторизация не пройдёт. Настройка
описана в `docs/google-tasks-setup.md`.

**Заработало 11 августа 2026:** `sync: authorized`, `sync: uploaded=2`.

- [x] Google Cloud Console настроен по инструкции, SHA-1 debug-ключа
      зарегистрирован.
- [x] «Подключить Google Tasks» открывает выбор аккаунта и экран согласия.
- [x] Напоминания уходят в Google: две записи выгрузились за секунду.
- [x] Напоминания, созданные **до** подключения, тоже выгружаются — обе
      выгруженные записи были созданы задолго до авторизации.
- [ ] Задача видна на tasks.google.com (приложение отчиталось об успехе,
      глазами в вебе не смотрели).
- [ ] Отметка «выполнено» в приложении перекрашивает задачу в Google.
- [ ] Удаление напоминания удаляет задачу в Google.
- [ ] В самолётном режиме синхронизация не падает, а повторяется позже.
- [ ] Отзыв доступа в настройках Google-аккаунта показывает ошибку в
      настройках приложения, а не тихо ломает выгрузку.

### Этап 7 — самодиагностика
- [x] Вкладка «Настройки» показывает шесть проверок, состояние совпадает с
      реальными настройками телефона: сверено с `dumpsys` по каждой.
      Список проверок прокручивается внутри себя — с первого взгляда видно
      четыре из шести, остальные два ниже.
- [x] Кнопка «Исправить» открывает нужный системный экран. Проверена на
      единственной горевшей проверке — оптимизации батареи. Ведёт в
      `ACTION_IGNORE_BATTERY_OPTIMIZATION_SETTINGS`, то есть в общий список
      **всех** приложений, где «Сороку» надо искать по алфавиту. Это
      сознательный выбор: адресный диалог требует отдельного разрешения.
- [x] После возврата из настроек список сам обновляется, без перезапуска —
      выключенный снаружи канал отражается сразу по возвращении.
- [x] Тестовое уведомление приходит через минуту: будильник на 13:46:35,
      уведомление в 13:46:37 при погашенном экране.
- [x] Если выключить канал «Напоминания» в системе — проверка это ловит:
      «Канал выключен, уведомления этого типа отбрасываются молча».

      **Осторожно при повторе:** выключение и включение канала через системные
      настройки сбрасывает важность с HIGH до LOW, а поднять её обратно
      приложение уже не может — `createNotificationChannel` существующий канал
      не повышает. Возвращать вручную: «По умолчанию» + «Всплывающее окно».

### Этап 6 — таблетки
- [x] Курс заводится: название, «100, 75», время, дата начала. Сегодняшняя
      доза на карточке совпадает с ожидаемой (первый день цикла — 100 мг).
- [x] Уведомление приходит в назначенное время и показывает три кнопки:
      «Принял», «+15 мин», «+60 мин». **Сработало из глубокого Doze**:
      будильник на 13:20:00, уведомление в 13:20:00.062 — опоздание 62 мс.
      Кнопки видно только после разворачивания: два уведомления приложения
      система свернула в группу.
- [x] «+15 мин» откладывает и не ломает будильник на завтра: в `dumpsys alarm`
      одновременно живут отсрочка на 13:38 и ежедневный на завтра 13:20.
- [x] «Принял» закрывает уведомление, карточка и история обновляются.
- [x] Полный набор отсрочек (5/10/15/30/60) присутствует на экране приложения
      (кнопки не нажимали, проверено наличие).
- [ ] Пропущенный день виден в истории как «пропущено» и отмечается задним
      числом — с дозой того дня, а не сегодняшней.
- [x] Доза чередуется 100 → 75 — покрыто юнит-тестами `DoseCycleTest`;
      за два реальных дня подряд не проверялось.
- [x] После перезагрузки телефона будильник приёма восстанавливается.

### Этап 5 — быстрые точки входа
- [x] Плитки «Напоминание» и «Покупка» добавляются в шторку и ведут прямо
      в распознавание. Добавлены через `cmd statusbar add-tile`, руками из
      редактора шторки не перетаскивали.
- [x] С заблокированного экрана плитка сначала просит разблокировать —
      это ожидаемо, распознавание поверх лока не работает. Тап по плитке
      поднял запрос отпечатка, keyguard остался на месте.
- [ ] Долгий тап по иконке приложения показывает два шортката, оба
      открывают нужный тип записи.
- [x] Виджет напоминаний лежит на домашнем экране и работает. Проверен
      12 августа, уже в новом виде — см. отдельный список ниже.
- [ ] Запуск покупки, когда экран диктовки напоминания ещё открыт (и
      наоборот), переключает тип, а не молча оставляет старый.

### Этап 4 — напоминания
- [ ] «завтра в 9 позвонить в поликлинику» с клавиатуры и голосом даёт
      одинаковый результат. Русскую фразу через adb не набрать; английский
      эквивалент («in 3 minutes call mom») разобран верно: заголовок
      «call mom», время — ровно через три минуты.
- [x] Уведомление приходит в назначенное время, в том числе когда телефон
      лежал в Doze. Устройство загонялось в `deviceidle force-idle`:
      будильник на 13:08:00, срабатывание в 13:08:00.070. В `dumpsys alarm`
      у будильника `flags=0x5` (STANDALONE + ALLOW_WHILE_IDLE) и
      `device_idle=--` — Doze его не придерживает.
- [x] Кнопка «Готово» в уведомлении закрывает его и отмечает напоминание.
      Проверено 12 августа: напоминание в списке стало зачёркнутым сразу.
- [x] Повторяющееся («каждый вторник») разбирается: «11 авг., 09:00 · по вт».
      Переезд на следующую неделю **после** срабатывания не наблюдали —
      ждать до вторника.
- [x] После перезагрузки телефона будущие напоминания продолжают работать:
      все три будильника вернулись с теми же `origWhen`.
- [x] Разрешение на уведомления запрашивается при первом запуске.
- [x] Точные будильники: система выдала разрешение сама — в `dumpsys alarm`
      стоит `exactAllowReason=policy_permission`, то есть сработал
      `USE_EXACT_ALARM` без похода в настройки.

### Этап 3 — виджет покупок
Ни один пункт не проверен: положить виджет на домашний экран можно только
руками, adb этого не умеет. Оба провайдера при этом системе видны —
в `dumpsys appwidget` есть и `ShoppingWidgetReceiver`, и
`ReminderVoiceWidgetReceiver`, так что до перетаскивания дело дойдёт.

- [ ] Виджет добавляется на домашний экран и показывает актуальный список.
- [ ] Тап по позиции отмечает её купленной, изменение видно и в приложении.
- [ ] Изменение списка в приложении обновляет виджет без ручного обновления.
- [ ] Кнопка микрофона на виджете ведёт сразу в распознавание.
- [ ] Виджет переживает перезагрузку телефона.

## 9. Текущий статус

**Этап 0 выполнен:** каркас проекта, Compose + Material 3, Room со схемой всех
четырёх таблиц, нижняя навигация по трём разделам, локализация RU/EN,
адаптивная иконка, CI с юнит-тестами и debug-APK.

**Этап 1 выполнен:** список покупок — ввод с клавиатуры, отметка «куплено»,
удаление свайпом, массовое удаление купленного, пустое состояние.
Слои: `ShoppingRepository` → `ShoppingViewModel` → `ShoppingScreen`.

**Этап 2 выполнен:** голосовой ввод. `VoiceCaptureActivity` — прозрачная
активность в отдельной задаче, сразу открывает системный диалог распознавания;
`ShoppingPhraseParser` режет фразу на позиции по запятым и союзам; экран
подтверждения с правкой и удалением позиций. Кнопка микрофона на экране
покупок. Точка входа задаёт тип записи через `VoiceTarget`.

**Этап 3 выполнен:** виджет покупок на Glance. Читает тот же поток Room, что и
экран, поэтому обновляется сам — `updatePeriodMillis` нулевой, периодических
пробуждений нет. Тап по позиции отмечает купленной через `ActionCallback`,
кнопка микрофона ведёт в `VoiceCaptureActivity`.

**Этап 4 выполнен:** напоминания. `ReminderPhraseParser` разбирает даты,
время и повторы на правилах; `AlarmManager.setExactAndAllowWhileIdle` через
интерфейс `ReminderScheduler` (Android-реализация подменяется в тестах);
уведомление с кнопкой «Готово»; пересоздание будильников по `BOOT_COMPLETED`
и `MY_PACKAGE_REPLACED`. Голосовой ввод напоминаний через тот же
`VoiceCaptureActivity` с `VoiceTarget.REMINDER`.

Взято разрешение `USE_EXACT_ALARM` (выдаётся системой без участия
пользователя). Play его разрешает только будильникам и календарям, но
приложение в Play не публикуется — иначе пришлось бы каждый раз ходить в
системные настройки за `SCHEDULE_EXACT_ALARM`.

**Этап 5 выполнен:** быстрые точки входа. Две плитки в шторке
(`TileService`), два статических шортката, виджет-кнопка напоминаний.
Цель различается по intent-action (`…action.CAPTURE_SHOPPING` /
`CAPTURE_REMINDER`), а не по extra: шорткаты так надёжнее. Из-за шорткатов
`VoiceCaptureActivity` пришлось сделать `exported="true"`.

**Этап 6 выполнен:** таблетки. `DoseCycle` вычисляет дозу из даты (тесты
обязательны и написаны), `MedRepository` ведёт курс, приёмы и историю,
`AlarmManagerMedScheduler` держит два раздельных будильника — ежедневный и
отложенный, чтобы отсрочка не затирала завтрашний. Уведомление с тремя
кнопками, полный набор отсрочек внутри приложения, история с отметкой задним
числом.

Каркас истории строится из цикла доз, а не из записей БД: проигнорированный
день записи не создаёт и без каркаса просто исчез бы из истории.

**Этап 7 выполнен:** вкладка «Настройки» с самодиагностикой. Шесть проверок
(уведомления, два канала по отдельности, точные будильники, оптимизация
батареи, наличие распознавателя), кнопка «Исправить» открывает нужный
системный экран, список перечитывается при каждом возврате на экран.
Тестовое уведомление через минуту идёт тем же путём, что и настоящее:
точный будильник → пробуждение из Doze → показ.

Разделов в нижней навигации стало четыре. Сюда же лягут настройки Google
(этап 8) и поле API-ключа (этап 9).

**Этап 8 выполнен:** односторонняя выгрузка напоминаний в Google Tasks.
Авторизация через Authorization API (Play Services), обращение к REST API
напрямую через Retrofit, выгрузка в `SyncWorker` (WorkManager, повтор с
backoff), настройки и статус на вкладке «Настройки».

Ключей в коде нет: Android-клиент OAuth определяется парой «пакет + SHA-1».
Отсюда ограничение — APK из CI авторизацию не проходит, синхронизация
проверяется только на локальной сборке. Инструкция по настройке Google Cloud
Console: `docs/google-tasks-setup.md`.

Google Tasks хранит только дату, время задачи отбрасывается на их стороне.
Само напоминание в приложении срабатывает в точное время.

**Этап 9 выполнен:** гибридный разбор фраз. Интерфейс `PhraseParser` с двумя
реализациями: `RuleBasedPhraseParser` и `LlmPhraseParser` (GLM-4.7-Flash
прямым HTTP-вызовом). `HybridPhraseParser` зовёт LLM только когда правила не
нашли во фразе ничего про время — для этого у `ParsedReminder` появился
признак `isConfident`. Если LLM недоступна или ответила мусором, отдаётся
результат правил: напоминание не теряется никогда.

Ключ Z.ai вводится в настройках и хранится в DataStore. Обратно в
интерфейс он не поднимается — экран знает только, задан ключ или нет.

**Этап 10 выполнен:** полировка. Закрыты все ограничения, записанные на
предыдущих этапах: отмена удаления через общий Snackbar, правка напоминания
(заголовок, дата, время), пикеры вместо текстового ввода времени и даты
начала курса. Экспорт истории приёмов был здесь же и позже убран — см. ниже.

Отмена удаления не откладывает удаление, а восстанавливает запись: приём с
таймером рассыпается, если приложение закрыть до его истечения.

**Все десять этапов плана пройдены.**

**10 августа 2026 — первый прогон на живом Pixel 8 (Android 17).** Разобрана
примерно половина раздела 8; всё, что не требует рук, проверено через adb.

Подтвердилось главное, ради чего затевались точные будильники: и напоминание,
и приём таблетки срабатывают из **глубокого Doze** с опозданием в десятки
миллисекунд, а после перезагрузки все будильники возвращаются с прежним
временем.

Что нашлось и починено:

1. **CI был красный два коммита подряд.** `android.util.Log` в юнит-тестах —
   заглушка, бросающая исключение; после коммита с логированием падало 15
   тестов из 112. Внутри корутин это выглядело как «состояние не то», а не
   как ошибка логирования.
2. **Уведомление о приёме возвращалось после «Принял».** Отсрочка не
   снимается при отметке, а `onAlarm` отдавал приём любого статуса. Теперь
   решает статус приёма. Заодно закрылся случай отметки задним числом.
3. **`scripts/device-check.sh` не работал ни одной из трёх ключевых частей:**
   не находил adb (Android Studio не кладёт его в PATH), проскакивал все
   одиннадцать вопросов к человеку при отсутствии терминала, писал при этом
   правдоподобный отчёт, и опрашивал несуществующий сервис
   `notification_manager` вместо `notification`.

Что выяснилось про окружение: телефон уже на **Android 17 (API 37)** при
`targetSdk 35`, а собрать проект можно только JDK из Android Studio —
системный JDK 26 несовместим с Gradle 8.9. Версии сборки при этом не трогали.

Открытый вопрос про запятые закрыт, и ответ отрицательный — подробности в
разделе 8, этап 2.

Дальше: пункты раздела 8, требующие рук владельца, плюс этапы 8 и 9,
у которых нет ни ключа LLM-провайдера, ни настроенной Google Cloud Console.

**Смена LLM-провайдера (после первого прогона).** Claude Haiku заменён на
бесплатный GLM-4.7-Flash от Z.ai. Причина не только цена: владелец ходит в
сеть из России без VPN, а `api.anthropic.com` оттуда недоступен — то есть
этап 9 был нерабочим в принципе, а не просто неоплаченным. Из бесплатных
тарифов по той же причине отпали Google AI Studio, Groq и GitHub Models.

Клиент переписан с формата Anthropic на OpenAI-совместимый
`chat/completions` (`OpenAiCompatApi`), поэтому дальнейшая смена провайдера —
это две константы. Старый ключ Anthropic из DataStore не мигрируется, а
стирается: у нового провайдера он молча дал бы 401.

Четыре вещи, найденные на живом ключе, — все стоили бы вечера каждая:

1. **Базовый адрес — `https://api.z.ai/api/paas/v4/`, а не разрекламированный
   `/api/openai/v1/`.** Второй на этом ключе отдаёт
   `{"code":500,"msg":"404 NOT_FOUND"}` на любой путь, включая `/models`,
   причём с HTTP 200 — то есть проверка «эндпоинт жив» по коду ответа врёт.
   Схема тел при этом всё равно OpenAI-совместимая.
2. **`thinking: {"type":"disabled"}` обязателен, а не для скорости.** У GLM
   рассуждения включены по умолчанию и на этой задаче съедают весь бюджет
   `max_tokens`: ответ приходит с `finish_reason: length`, пустым `content` и
   тремя тысячами знаков в `reasoning_content`. С `disabled` тот же запрос
   укладывается в 41 токен. Отсюда же `MAX_TOKENS = 1024` вместо прежних 300.
3. **`glm-4.7-flash` в списке `GET /models` отсутствует**, но работает и денег
   не просит. В списке только платные модели; `glm-4.5-air`, например,
   отвечает «Insufficient balance».
4. **Промпту нужны примеры, но без дат.** GLM слабее Haiku в следовании
   инструкциям: по старому промпту он тащил в `title` слова о времени
   («посылку бы забрать на неделе» целиком) и один раз поставил `dueAt` равным
   текущему моменту. Примеры чинят это, но с конкретными датами модель
   начинает их копировать вместо счёта от «сейчас» — поэтому в примерах
   показан только разбор `title`.

`LlmPhraseParser` теперь логирует неудачу вызова (`llm: call failed`) и пустой
`content`. Раньше любая беда — не тот адрес, отозванный ключ, нет сети —
выглядела одинаково: молчаливый откат на правила, по которому не понять
вообще ничего.

**Редизайн «тёплое стекло» (11 августа 2026).** Интерфейс переписан с
умолчаний Material 3 на собственный визуальный язык: градиентный фон из трёх
радиальных пятен, стеклянные карточки, плавающая навигация, строка ввода
таблеткой, каскадное появление списков, движение на пружинах. Обоснования
решений — в разделе 3, «Оформление».

Затронуты все четыре вкладки, карточка диктовки и оба виджета. Виджеты
получили палитру и типографику, но не материал: Glance этого не умеет.

**Настройки внешнего вида.** Тема и язык выбираются внутри приложения
(`AppearancePreferences` в своём DataStore, применяются в `MagpieRoot` —
общей обёртке всех точек входа с интерфейсом). Обёртка общая не для красоты:
экран диктовки запускается с виджета и плитки в обход главного экрана и без
неё выглядел бы в системной теме, пока остальное приложение — в выбранной.

Две ловушки смены языка, обе стоили падения приложения:

1. **`LocaleManager` (Android 13+) применить нельзя.** Запись
   `applicationLocales` **пересоздаёт активность**, поэтому вызов из эффекта
   композиции запускается заново на новой активности. Защита «сравнить с
   текущим значением» держится на том, что система вернёт записанное, — на
   Android 17 она не вернула, и получился бесконечный цикл пересозданий.
   Вместо этого подменяется конфигурация композиции: она ничего не
   пересоздаёт и зациклиться не может. Цена — переводится только то, что
   рисует Compose; остальное приходится переводить отдельно. Уведомления
   так и сделаны (см. «Уведомления на языке приложения» ниже), а вот
   **подпись под иконкой остаётся на языке системы** — её меняет только
   системный экран «Язык приложения» на Android 13+ (работает благодаря
   `android:localeConfig`).
2. **`ContextThemeWrapper`, а не `createConfigurationContext`.** Последний
   возвращает контекст, в цепочке которого нет активности, и всё, что ищет
   владельца подъёмом по `baseContext` (`rememberLauncherForActivityResult` и
   прочие `LocalActivity*`), на нём падает. «Настройки» с их запросом согласия
   Google так и уронили приложение: `No ActivityResultRegistryOwner was
   provided`.
3. **Диалог подмену не наследует.** Он рисуется в своём окне: содержимое
   компонуется внутри отдельного `AbstractComposeView`, а тот в корне своей
   композиции заново раздаёт андроидные локали из контекста собственного
   окна — и затирает наши. Провайдер снаружи `AlertDialog` не помогает,
   потому что окно создаётся внутри него. Поэтому язык снимается до входа в
   диалог и раздаётся заново в каждом слоте: `MagpieAlertDialog` в
   `core/ui/MagpieDialogs.kt`. Симптом был выразительный — английский
   интерфейс и полностью русский диалог правки поверх него.

**Размытие задника у карточки диктовки.** `blurBehindWindow()` включает
`FLAG_BLUR_BEHIND` и подписывается на отключение размытия системой. Это
единственное место в приложении, где размытие достижимо, и единственное, где
карточка стеклянная: без размытия сквозь заливку в 13% белого просвечивала
плавающая навигация прямо под кнопками «Закрыть» и «Повторить». Подробности —
в разделе 3.

**Главной веткой стала `main`.** Прежняя `claude/android-voice-assistant-88u9na`
удалена с обеих сторон, дефолтная ветка на GitHub переключена. Работа идёт в
ветках от `main` и возвращается пул-реквестом.

**Прогон редизайна на Pixel 8 (11 августа 2026).** Размытие и запасной путь
работают: с флагом `disable_window_blurs` размытие гаснет **на ходу**, без
перезапуска, — слушатель вместо проверки `SDK_INT` окупился. Радиус 24dp
оставлен как есть.

Прогон нашёл пять дефектов, все починены.

1. **Иконки статус-бара не следовали теме приложения.** `enableEdgeToEdge()`
   решает светлоту один раз при создании активности и по СИСТЕМНОЙ теме. Пока
   приложение всегда шло за системой, этого хватало; со своей настройкой темы
   светлая тема на тёмной системе дала белые часы на светлом фоне. Теперь
   контроллер инсетов правится в `SideEffect` внутри `MagpieTheme`.
2. **Смена языка сбрасывала вкладку.** Ранний выход при «как в системе»
   вызывал `content()` из другого места дерева, чем ветка с
   `CompositionLocalProvider`; переключение переносило поддерево, и Compose
   выбрасывал его вместе с состоянием `NavHost`.
3. **Виджет покупок показывал пустой список** и не чинился даже
   принудительным `APPWIDGET_UPDATE`. Две причины сразу: Glance публикует
   первый скомпонованный кадр, а в нём было пустое `initial` у
   `collectAsState`, пока Room ещё читал; и подписка на Room обновляет виджет
   только пока жива сессия Glance, а она умирает вместе с процессом. Теперь
   список читается до `provideContent`, а запись в него толкает виджет из
   репозитория — единственного места, через которое проходят все, включая
   голосовой ввод.
4. **Навбар и карточка диктовки лежали на жест-полосе:** не съедали инсет
   навигационной панели.
5. **Карточка диктовки просвечивала, а диалоги были не того цвета.**
   Подробности и замеры — в разделе 3, «Оформление». Оба случая наглядны тем,
   что глазу они кажутся одной и той же болезнью, а причины разные.

**lint запускается в CI (11 августа 2026).** Раньше его не делал никто:
локально мешал `--offline`, в CI шага не было. Из 4 ошибок первого запуска
настоящих не оказалось — все подавлены точечно, с записанной причиной, а не
общим baseline. `NewApi` был чист сразу: вызовы API 31 в `WindowBlur`
закрыты верно.

Из 65 предупреждений разобрано 13; существеннее прочих —
`UnsafeIntentLaunch`: `VoiceCaptureActivity` пересылала дальше полученный
интент, а она `exported` ради шорткатов. Теперь интент собирается заново из
разобранной цели. Оставшиеся 52 — все об одном: зависимости устарели. Они
зафиксированы намеренно, поэтому оставлены видимыми.

Один совет lint оказался вредным и записан в `app/lint.xml`: слияние
`mipmap-anydpi-v26` в `mipmap-anydpi` ломает сборку — AAPT перестаёт находить
`ic_launcher`.

**Синхронизация с Google Tasks заработала (11 августа 2026).** Этап 8 был
написан 10 августа и до сих пор ни разу не проходил целиком. В логе
`sync: authorized` и `sync: uploaded=2`; выгрузились обе записи, созданные
задолго до подключения.

Мешали три вещи подряд, и каждая маскировала следующую:

1. **Кнопка «Синхронизировать сейчас» ставила задачу в очередь**, а фоновая
   задача не может показать экран согласия — у неё нет активности. Она
   упиралась в `consent_required`, записывала ошибку и завершалась, сколько
   ни нажимай. Теперь авторизация спрашивается на экране, как и у
   «Подключить», а очередь ставится уже после получения доступа.
2. **Проект в Google Cloud Console не был настроен вовсе.** Здесь легко
   ошибиться в диагнозе: выбор аккаунта показывается и без настроенного
   проекта, так что дойти до экрана согласия — ещё не признак того, что
   консоль готова.
3. **Аккаунт не был в списке Test users.** Google отвечает на это
   `403: access_denied` с формулировкой «has not completed the Google
   verification process», хотя проходить верификацию не нужно: она требуется
   только для публикации на всех пользователей. В режиме Testing достаточно
   добавить аккаунт в список.

Отсюда правило на будущее: **каждый аккаунт, с которого будут пользоваться,
добавляется в Test users** — свой и родных, кому раздан APK. Лимит 100.

**Разбор покупок через LLM (11 августа 2026).** Связка «правила → LLM»
продлена на покупки; открытый вопрос про запятые, висевший с 10 августа,
закрыт. Обоснование — в разделе 2.

Три ловушки, каждая стоила отдельного прогона на устройстве:

1. **Союз — не граница.** Первая версия шла к модели, только когда правила
   вернули ровно одну позицию. «надо молоко мясо колбаса сыр и салфетки»
   правила разрезали по «и» на две — к модели не пошли, и первая осталась
   комом из четырёх продуктов.
2. **Промпту нельзя доверять чистку.** Разрезав фразу верно, модель вернула
   «купить хлеб» и «и наверное ещё фарш»: просьбу выбросить вводные слова она
   выполняет примерно через раз. Поэтому её результат проходит через ту же
   регулярку, что и правила.
3. **`\b` и `IGNORE_CASE` не работают с кириллицей.** В Java словарными
   символами по умолчанию считается только латиница, поэтому границы слова
   после «купить» нет вовсе, а регистр складывается тоже лишь у латиницы.
   Отсюда `(?iu)` и взгляд вперёд на пробел вместо `\b`. Обе ошибки
   молчаливые: регулярка просто ничего не находит.

**Потеря позиций при сохранении — та же правка, обратная сторона.**
`onConfirm` выставлял `Done` до записи, активность на `Done` закрывается, и
`viewModelScope` отменялся посреди цикла. Порядок был неверным всегда, но
проявился только когда запись подорожала: из пяти покупок стали сохраняться
три. Запись теперь заканчивается до `Done`, от второго тапа защищает флаг, а
массовая вставка трогает виджет один раз вместо раза на позицию.

**Экран настроек упрощён.** Список из шести проверок заменён карточкой,
которая появляется, только если что-то реально мешает доставке. Постоянная
панель самодиагностики — не то, что делают в приложениях: пять всегда зелёных
строк приучают не читать шестую.

Кнопка «Исправить» у оптимизации батареи открывает адресный системный диалог
вместо общего списка всех приложений. Из-за списка проверка выглядела
сломанной: рядом с нужным переключателем стоит «ограничить фоновую
активность», а это другая настройка — в белый список Doze кладёт только «Без
ограничений», и читает проверка именно его. Для адресного диалога понадобился
`REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`; рассуждение то же, что и с
`USE_EXACT_ALARM` — Play его ограничивает, но приложение в Play не
публикуется.

**Экспорт истории приёмов убран** — не понадобился.

**Виджеты остались на системной теме.** Попытка привязать их к настройке
приложения компилировалась и на бумаге была верна, но на устройстве эффекта
не дала, а причину найти не удалось. Виджет, который обещает следовать теме
приложения и не следует, хуже виджета, который открыто следует системе.
Размен принят сознательно: светлое приложение может соседствовать с тёмными
виджетами.

**Уведомления на языке приложения (12 августа 2026).** Первый пункт списка
из `docs/next-steps.md`. Строки уведомлений собираются из контекста с
выбранной локалью (`core/settings/AppLocale.kt`), а не из системного:
`forSelectedLanguage()` читает язык из `AppearancePreferences` и отдаёт
контекст через `createConfigurationContext`. Здесь он уместен — запрет из
ловушек смены языка касается Compose, где по цепочке `baseContext` ищут
активность; строителю уведомления активность не нужна.

Язык читается в `MagpieNotifications`, а не в получателях: получателей три
(напоминание, доза, тестовое), и четвёртый забыть легко. Отсюда `showDose` и
`showReminder` стали `suspend` — все вызывающие и так работают в корутине под
`goAsync`.

**Имена каналов следуют настройке.** Их человек видит в системных настройках,
и оставить их на языке системы значило бы получить «Reminders» в приложении и
«Напоминания» в настройках. Каналы создаются в `MagpieApp` дважды: сразу и
синхронно на языке системы (процесс нередко поднимает будильник, а канал
обязан существовать до первого уведомления), а затем подпиской на
`AppearancePreferences.language` — `createNotificationChannel` существующему
каналу имя обновляет. Важность при этом не поднимает, и это отдельная старая
ловушка, записанная в этапе 7.

**Виджет напоминаний показывает ближайшее (12 августа 2026).** Второй пункт
списка из `docs/next-steps.md`. Вместо кнопки во всю ячейку виджет показывает
заголовок и время ближайшего невыполненного напоминания; микрофон ушёл в
угол. Прежнюю работу «одна большая цель для пальца» делают шорткат и плитка в
шторке, а виджет занимал место как полноценный и не показывал ничего.

**Просроченные не отсеиваются.** Виджет показывает первое по времени
невыполненное, даже если срок прошёл: это ровно то, что человеку и нужно
увидеть. Вдобавок отбор «только будущие» требовал бы знать «сейчас», а виджет
живёт кадрами и сам по времени не обновляется.

**Две раскладки.** Виджет мог быть положен на экран ещё кнопкой, и в ячейку
1×1 текст не влезает — от него остаётся обрезанный микрофон, что и увидели на
устройстве. Поэтому `SizeMode.Responsive`: узкий размер остаётся кнопкой,
широкий показывает напоминание.

**Толчок при записи — тот же хук, что у покупок.** `ReminderRepository`
получил `onChanged`, пять мест записи. Причина прежняя: подписка на Room
обновляет виджет, только пока жива сессия Glance, а она умирает вместе с
процессом.

**Смена языка тоже толкает виджеты.** Для виджета язык — такое же изменение,
как новая запись: сам он не перерисовывается и остался бы на прежнем языке до
ближайшей правки данных. Толчок стоит в том же слушателе `MagpieApp`, что
переименовывает каналы. Обновление доходит за несколько секунд, а не
мгновенно.
